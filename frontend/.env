# GameArena Frontend Environment Variables

# Flask API URL (backend server)
# En mode développement avec Vite, laisser vide pour utiliser le proxy
# Le proxy Vite redirige automatiquement /api/* vers http://127.0.0.1:3000
# Pas de CORS car le navigateur voit tout comme même origine !
# VITE_API_BASE_URL=http://127.0.0.1:3000  # ❌ Cause des erreurs CORS
VITE_API_BASE_URL=

