{
  "name": "gamearena-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:copy": "vite build && BACKUP_DIR=../static_backup_$(date +%Y%m%d_%H%M%S) && mkdir -p \"$BACKUP_DIR\" && (cp -r ../static/* \"$BACKUP_DIR\" || true) && rm -rf ../static/* && cp -r dist/* ../static/",
    "preview": "vite preview --port 5173"
  },
  "dependencies": {
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "@monaco-editor/react": "^4.4.6",
    "axios": "1.4.0"
  },
  "devDependencies": {
    "vite": "5.0.0",
    "@vitejs/plugin-react": "^4.0.0"
  }
}
