# GameArena - Fichier d'environnement pour Docker
# Copier ce fichier vers .env et modifier les valeurs

# ============================================
# Secrets (OBLIGATOIRE À CHANGER EN PRODUCTION)
# ============================================
SECRET_KEY=change-this-secret-key-in-production-use-random-string
JWT_SECRET_KEY=change-this-jwt-secret-in-production-use-random-string

# ============================================
# Database Configuration
# ============================================
# SQLite (par défaut, simple pour dev/petite prod)
DATABASE_URL=sqlite:///gamearena.db

# PostgreSQL (recommandé pour production)
# Décommenter et configurer si vous utilisez le service postgres dans docker-compose.yml
# DB_PASSWORD=your-secure-database-password
# DATABASE_URL=postgresql://gamearena:${DB_PASSWORD}@postgres:5432/gamearena

# ============================================
# Bot Execution Configuration
# ============================================
# Mode d'exécution des bots: auto | docker | subprocess
# - auto: essaie docker, puis subprocess si docker indisponible
# - docker: force l'utilisation de conteneurs Docker (plus sécurisé)
# - subprocess: exécute directement les bots (rapide mais moins sécurisé)
BOT_RUNNER=auto

# Image Docker pour les bots (si BOT_RUNNER=docker ou auto)
BOT_DOCKER_IMAGE=gamearena-bot:latest

# Répertoire temporaire pour l'exécution des bots
BOT_TMP_DIR=/tmp/bot_runs

# Timeouts (ms)
BOT_DOCKER_AUTO_THRESHOLD_MS=500
BOT_DOCKER_STARTUP_MS=100

# ============================================
# Application Configuration
# ============================================
# Port d'écoute (ne pas changer si vous utilisez docker-compose)
PORT=3000

# Environment (development | production)
FLASK_ENV=production

# ============================================
# Logs et Debugging
# ============================================
# Activer le tracing Docker runner (debug)
DOCKER_RUNNER_TRACE=false
DOCKER_RUNNER_PER_RUN_CHECKS=false

# ============================================
# Sécurité
# ============================================
# CORS Origins (séparés par des virgules)
# En production, restreindre aux domaines autorisés
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Limites de requêtes (rate limiting) - À implémenter
# MAX_REQUESTS_PER_MINUTE=60
# MAX_REQUESTS_PER_HOUR=1000
