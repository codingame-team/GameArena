Flask==2.3.2
python-dotenv==1.0.0
Flask-Cors==4.0.0
Flask-SQLAlchemy==3.0.5
Flask-JWT-Extended==4.5.2
werkzeug==2.3.6
bcrypt==4.0.1

# Note: This is a minimal prototype. In production you should run bots in a strong sandbox (containers, seccomp, gVisor...).
